# Skill-06

Aim:

Implement escape-sequence handling and command execution using fork() and exec().

Files:

- skill-06.c
- observations.txt

Compile:

gcc skill-06.c -o skill-06

Run:

./skill-06

Features:

- Detects escape sequences
- Executes Linux commands
- Creates child processes
- Handles execution errors
- Parent waits for child completion
