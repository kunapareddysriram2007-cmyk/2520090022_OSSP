# Skill-12

Aim:

Implement command pipelines using pipe(), fork(), dup2(), and exec().

Files:

- skill-12.c
- observations.txt

Compile:

gcc skill-12.c -o skill-12

Run:

./skill-12

Features:

- Creates a pipe
- Redirects input/output streams
- Executes connected commands
- Closes unused file descriptors
- Synchronizes child processes
- Demonstrates pipeline execution
